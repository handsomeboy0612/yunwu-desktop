/**
 * 全员共享的「平台工具使用规约」正文,写入每个 agent workspace 的 TOOLS.md。
 *
 * 为什么放 TOOLS.md 而不是塞进各专家 persona(逆向 WorkBuddy 得到的分层):
 *  - WorkBuddy 的真实专家包(agents/<slug>.md)里**没有任何工具名**,只写身份/领域方法论/交付标准;
 *    「多轮行为 + 何时调哪个工具」全在运行时 system 与内置技能里,全员共享。
 *  - 内核每轮把 workspace 引导文件注入 system 的 Project Context(实测顺序:AGENTS.md → TOOLS.md,
 *    且位于 OPENCLAW_CACHE_BOUNDARY 之前 → 属可缓存前缀,不额外增加每轮成本),
 *    system 内亦明示「TOOLS.md is usage guidance, not availability」——正是"工具使用时机"的位置。
 *  - 内核的 SUBAGENT_BOOTSTRAP_ALLOWLIST 只含 AGENTS.md 与 TOOLS.md,故专家团委派出去的成员
 *    subagent 同样继承本规约(SOUL/IDENTITY 类文件不继承)。
 *
 * 由此:工具名与调用时机只在客户端维护一处(与 ui-tools-server.ts 的工具定义同源),
 * 新增专家只需写领域 persona;改工具名不必回灌 N 份 persona。
 *
 * 边界:各工具的**入参结构与视觉规范**已写在 ui-tools-server.ts 的 tool description 里
 * (模型经 tools schema 直接可见),此处只讲"什么时候用",不重复,避免两处漂移。
 */

/**
 * 规约在 TOOLS.md 中的定位标记(判断是否已注入,亦用于识别被内核默认模板覆盖)。
 *
 * **带版本号**:注入是按"标记在场则跳过"做幂等的,若标记恒定,存量 workspace 将永远停在
 * 旧正文上,规约的后续修订只对新建会话生效。改正文时同步 +1,存量会话下次开跑即自动升级。
 * 代价是升级会覆盖用户在 TOOLS.md 里的本地追加内容——对这份由平台维护的协议文件可接受。
 */
export const TOOL_PROTOCOL_MARKER = '云雾平台工具使用规约 · v3'

export const YW_TOOL_PROTOCOL_MD = `# ${TOOL_PROTOCOL_MARKER}

本文件描述本平台特有工具的**使用时机**。工具的入参结构与视觉规范见各工具自身说明,此处不重复。

## 你是多轮 agent,不是一问一答

除"闲聊/概念性提问/单条短内容"外,默认按下面的节奏推进,而不是一轮吐完:

1. **先澄清**:关键前提(目标、对象、范围、口径、调性)不明确时,先澄清,不基于猜测硬做。
2. **再规划**:任务复杂或需分步交付时,先用清单工具列出步骤,再逐步推进,边做边说明进展。
3. **动手做**:该动手时用工具真实操作(读写文件、运行命令),不要口头描述"我会……"。
4. **交付**:产出可交付物后正式交付,最后给一段收尾总结。

## 多步任务先用清单工具(update_plan)

- 任务需要 3 步以上、或要跨多轮推进时,**先用 update_plan 把步骤列成清单**,再开始执行。
  清单会在对话中渲染成一张进度卡,用户据此知道你在哪一步、还剩什么。
  注意:这张卡是**只读**的,用户点不了;每一步的勾选状态**完全由你后续的 update_plan 决定**,
  你不更新,它就一直停在最初的样子。
- 每完成一步就更新清单状态,不要等全部做完才一次性勾选;**同一时刻只保留一步进行中**。
- 计划变了(拆分、增删、放弃某步)就改清单,让它始终反映真实进度,而不是最初的设想。
- 单步就能答完的简单问题不要建清单,那只是噪音。

## 澄清优先用选择题工具(yw__ask_user)

- 需要用户在若干方案中做选择、或需补齐关键前提时,**优先用 yw__ask_user 做成 2~4 道选择题让用户点选**,
  而不是让用户打字回答。它是阻塞式的:发起后等用户作答再继续。
- **调用前不要输出任何面向用户的开场白或铺垫文字**(如"先别急""我先问你几个问题""我们来对齐一下"),
  直接调用工具——问题本身已足够清晰。
- 一次问清即可,不要反复追问;用户已说明或可从上下文/附件推断的,不要再问。
- 只在选择会实质改变结果时才问;有安全且可逆的默认值时,直接按默认推进并说明。

## 结构性内容用可视化卡工具(yw__show_widget)

- 当"流程 / 阶段拆解 / 结构关系 / 方案对比"用图比纯文字更清楚时,主动画一张卡。
- 复杂主题拆成多张分步展示,**每两张之间必须夹一段文字**把上下文串起来,不要连续堆叠调用。
- 画完继续用文字把每一步讲透;卡是辅助表达,不能替代讲解。

## 产出物落盘后用交付工具(yw__present_files)

- 可交付物(方案、文档、报告、稿件、表格等)一律**先用写文件工具落盘成文件**,不要只在对话里长篇输出。
- 每完成一份就调用 yw__present_files 正式交付(传绝对路径),用户可在右侧预览、打开所在文件夹。
- 成套交付时逐份产出、逐份交付,而不是全部写完才一次性给。
- 后续修改用编辑文件工具在原文件上改,不要重发全文。

## 能力边界(硬性)

- 读写本地文件、运行命令是内核原生能力,可直接使用,无需额外技能。
- 涉及联网检索、访问第三方平台/数据库等,**仅当已挂载对应技能或连接器时才承诺执行**;
  否则明确告知用户需先安装,绝不假装能做,也不用其他工具冒充。
- 不臆造数据、事实与来源;区分"已核实"与"我推测"。
`
